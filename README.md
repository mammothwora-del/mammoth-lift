# Mammoth Lift 🏋️

A minimal, installable workout tracker PWA for a 5-day recomposition split (Push / Pull / Legs / Shoulders / Full Body). No backend, no build step — just open `index.html` or visit the hosted page.

## Features

- 5-day workout split with sets, reps, and weight tracking
- Remembers your last session per exercise and flags new PRs
- Daily streak counter
- Workout history view with per-day volume totals
- Multiple user profiles — switch between people sharing the same device, each with their own separate workout/nutrition history
- Equipment checklist (per person) and a form/posture guide for every exercise, grouped by Push / Pull / Legs / Shoulders / Full Body
- Installable as a PWA (Add to Home Screen) with offline support via a service worker
- All data stored locally in the browser (`localStorage`) — nothing leaves your device

## Usage

Just open `index.html` in a browser, or visit the GitHub Pages URL below. On mobile, use "Add to Home Screen" / the install prompt to use it like a native app.

## Live app

https://mammothwora-del.github.io/mammoth-lift/

## Tech

Single-file HTML/CSS/JS, no dependencies, no build step.
